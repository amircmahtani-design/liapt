rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Amir PT — unchanged.
    match /amirpt/{docId} {
      allow read, write: if request.auth != null;
    }

    // Train (Lia). Her own collection: her check-ins, cycle records,
    // measurements, pain reports and workout history never touch the
    // amirpt documents, and neither app reads the other's.
    match /liapt/{docId} {
      allow read, write: if request.auth != null;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
